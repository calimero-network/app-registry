name: Deploy to Production

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run tests
      run: npm test
    
    - name: Run linting
      run: npm run lint
    
    - name: Build Docker image
      run: |
        docker build -t ssapp-registry-backend:${{ github.ref_name }} .
        docker tag ssapp-registry-backend:${{ github.ref_name }} ssapp-registry-backend:latest
    
    - name: Deploy to production
      run: |
        echo "Deploying version ${{ github.ref_name }} to production"
        # Add your deployment commands here
        # Examples:
        # - Deploy to Kubernetes
        # - Deploy to AWS ECS
        # - Deploy to Google Cloud Run
        # - Deploy to Azure Container Instances
    
    - name: Notify deployment
      run: |
        echo "✅ Successfully deployed SSApp Registry Backend v${{ github.ref_name }}"
        echo "🌐 Health check: https://your-domain.com/healthz"
        echo "📚 API docs: https://your-domain.com/docs" 